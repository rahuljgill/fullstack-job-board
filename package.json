{
  "name": "jobboard",
  "version": "1.0.0",
  "description": "Full-stack Job Board - React + Tailwind + Laravel",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix frontend\" \"php backend/artisan serve --host=127.0.0.1 --port=8000\"",
    "frontend": "npm run dev --prefix frontend",
    "backend": "php backend/artisan serve --host=127.0.0.1 --port=8000"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
